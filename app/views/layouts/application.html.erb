<!DOCTYPE html>
<html>
  <head>
    <title><%= content_for?(:title) ? yield(:title) : "Untitled" %></title>
    <%= stylesheet_link_tag "application" %>
    <%= csrf_meta_tag %>
    <%= yield(:head) %>
    <link href='http://fonts.googleapis.com/css?family=IM+Fell+Great+Primer' rel='stylesheet' type='text/css'>
    <%=stylesheet_link_tag 'FloatBox' %>
    <style type="text/css">             
      header.main .title{
        font-family: 'IM Fell Great Primer', arial, serif;
      }
    </style>
  </head>
  <body>
      <header class='main'>
        <span class="title"><%=t('site_name')%> </span>
        <nav>
        <%if (!current_user)%>
          <%=link_to t('log_in'),new_user_session_path%> 
        <%else%>
          <%=link_to t('log_out'),destroy_user_session_path%> 
        <%end%>
        </nav>
      </header>
      <% flash.each do |name, msg| %>
        <%= content_tag :div, msg, :class=> "flash #{name}" %>
      <% end %>
      <%= content_tag :h1, yield(:title) if show_title? %>
      <%= yield %>
    <%= javascript_include_tag 'mootools-core-1.3'%>
    <%= javascript_include_tag 'mootools-more' %>
    <%= javascript_include_tag 'rails' %>
    <%= javascript_include_tag 'Post' %>
    <%= javascript_include_tag 'FloatBox' %>
    <%= javascript_include_tag 'application' %>
  </body>
</html>
